# Review: Chapter 8 (Reinforcement Learning), the book's final chapter

**Date:** 2026-06-07
**Scope:** chapter ch08 (Part III closer; its section 10 is the whole book's conclusion)
**Reviewers:** spec-auditor, quality-auditor, math-auditor, cross-ref-auditor (four parallel auditors, dispatched from the main loop)
**HEAD at review:** `8a1dce4` (draft) -> fixes applied this pass
**Branch:** `inductive-biases-part3`

## Auditor verdicts

| Auditor | Verdict | Summary |
|---|---|---|
| math-auditor | PASS | The bridge derivation (REINFORCE = return-weighted softmax cross-entropy) is **correct**, verified symbolically and numerically: score-function identity, trajectory factorization, the `p - onehot(a)` gradient, the **sign** (ascent on J = descent on the `-log pi * G` loss), and the return scaling. No sign error, no grad-log vs grad-neg-log conflation, no missing return weighting. All gridworld numbers reproduce exactly from `reinforce_gridworld.py`. |
| quality-auditor | Minor polish; **the book's ending is ship-ready** | Bridge lands as a genuine payoff; credit-assignment motivation precedes the math; gridworld earns its conclusions; AIXI mirrors Ch4's Solomonoff faithfully; section 10 works as a whole-book conclusion (concrete three-axes recap, honest result restated, resonant close). Two MEDIUM, three LOW. |
| spec-auditor | PASS | All 10 sections in order with correct labels; figure placed; book-wrap (decision B) confirmed; de-serialization complete (no blog idiom; count is eight; the lone "seven" is correct arithmetic); bridge derives; headers accurate. **Page budget: 12pp justified, do NOT trim** (the spec's own section sum was 9.75pp; the 9pp headline was loose). |
| cross-ref-auditor | PASS | 12 DEFINED labels unique book-wide; all 10 backward targets resolve; no forward/dangling refs; Ch7 omission intentional and clean; `make` exit 0, empty undefined baseline, 112 pages. |

No BLOCKING findings. The book's intellectual payoff is mathematically sound and its ending is strong.

## Findings and resolutions

| # | Sev | Location | Finding | Resolution |
|---|---|---|---|---|
| 1 | MED (x2: math + quality) | `02:89-92` | The reward-to-go reduction asserted "a reward before step t averages to zero" without showing why, the one underived step in a section that bills itself "a derivation, not an analogy." | FIXED. Grounded "averages to zero" in the zero-mean score identity E_a[grad log pi] = 0 (the same fact the baseline subsection proves), with the conditioning argument made explicit. |
| 2 | MED | `03:63-65` | Headline numbers (-0.1258, +0.9278) attributed to "the seed-fixed run" but the seed was not on the page; reproducibility lived entirely in the notebook. | FIXED. Added "seed 0" to the run description. (math-auditor independently reproduced the numbers at seed 0.) |
| 3 | LOW | `02:121-123` | The unbiasedness proof was given for a state-dependent baseline b(s) but the gridworld uses a scalar; the connection was left implicit. | FIXED. Added "a global constant is the trivial state-dependent baseline, so the same unbiasedness argument applies." |
| 4 | LOW | `04:43-44` | Forward-pointer "(the subject of the next section)" over-promised relative to what sec:rl-axis delivers (reward shaping as a prior, not the scalar-reward-as-commitment claim). | FIXED. Softened to point at the reward-design surface and its failure modes, which is what sec:rl-axis actually covers. |
| 5 | LOW (x2: spec + cross-ref) | `ch08.tex:9` | Header RESOLVED roster listed `sec:parallel-axes`, but the body never cites it (uses `sec:three-axes`). Stale list entry, not a broken ref. | FIXED. Dropped `sec:parallel-axes` from the header. |
| - | LOW | `04:111-112` | Compound modifier "identity-plus-squared-error" hyphenates across a line break. | LEFT AS-IS: cosmetic; the clean rebuild reports zero overfull boxes, so LaTeX set it without a stray hyphen. |

## Verification after fixes

- `make cleanall && make`: exit 0, **112 pages** (unchanged; the edits were small), zero overfull boxes.
- Zero undefined / multiply-defined references.
- `seed 0` present in the gridworld run; the zero-mean score identity present in the reward-to-go derivation; `sec:parallel-axes` removed from the ch08 header.
- Zero em-dashes across ch08.

## Status

**Chapter 8 PASS after fixes.** The bridge derivation is verified correct, the book's ending is ship-ready, and the 12pp budget is justified (no trim). The book is complete: eight chapters, three parts, 112 pages. Ready for the Part III integration pass and merge to `main`.
