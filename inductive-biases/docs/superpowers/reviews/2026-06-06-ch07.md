# Review: Chapter 7 (Reverse-Engineering the Pointer Transformer)

**Date:** 2026-06-06
**Scope:** chapter ch07 (Part III opener, the book's flagship chapter)
**Reviewers:** spec-auditor, quality-auditor, math-auditor, cross-ref-auditor (four parallel auditors, dispatched from the main loop because nested Task was unavailable to the drafting writer)
**HEAD at review:** `7a2ec05` (draft) -> fixes applied this pass
**Branch:** `inductive-biases-part3`

## Auditor verdicts

| Auditor | Verdict | Summary |
|---|---|---|
| spec-auditor | PASS | All 10 sections present in plan order, correct labels under `ch:interpretability`; three figures defined and placed in section 9; decisions D (two-models) and E (de-serialization) both satisfied (grep-verified); header blocks accurate; section-9 honest arc present. Non-blocking notes only. |
| cross-ref-auditor | PASS | Zero label collisions with Parts I-II; all five backward `\Cref` targets resolve; Chapter 8 forward ref is prose-only; `make` exit 0 with zero undefined / multiply-defined. Cross-ref map generated. |
| math-auditor | NEEDS CORRECTION (2 MINOR) | Every prose number matches the notebook's stored output or RESULTS.md exactly. Two provenance/rounding nits only (below). |
| quality-auditor | Minor polish, one bordering substantive | Arc lands; honest-empiricism genuine; voice consistent; no em-dashes; no banned phrases. One HIGH cross-chapter contradiction, four MEDIUM clarity items. |

No BLOCKING findings. The chapter is logically sound, builds clean, and every number is traceable.

## Findings and resolutions

| # | Sev | Location | Finding | Resolution |
|---|---|---|---|---|
| 1 | HIGH | `04:98-107` | §9 attributed the M=32 third layer to "a two-stage address aggregation... the five-bit assembly across layers 1 and 2", re-asserting the "depth = address bits" reading that Ch6 (`04-fix-and-verification.tex:140-152`) explicitly and in bold told the reader to reject (M=24 shares the 5-bit address yet needs only two layers; the real differentiator is one-of-M selection precision / lookup fan-out). A reader arriving from Ch6 is primed to catch the contradiction. | FIXED. Reworded to present the two-layer aggregation as an observation about *how this trained model used the depth it has*, not a claim that address width *required* the layer, with an explicit pointer to Ch6's selection-precision finding. |
| 2 | MED | `04:9-15` | "two layers cannot solve it at any width or training budget" overstated Ch6's hedged, frame-bounded claim. | FIXED. Now "in the controlled comparison there, depth solved the task where neither width nor a longer training budget did." |
| 3 | MED | `04:197-205` | The recovery formula used $\Delta_{\text{patched}}$, $\Delta_{\text{corrupt}}$, $\Delta_{\text{clean}}$ but defined only $\Delta$; "answer"/"other" undefined for the binary case. | FIXED. Added a sentence defining the three subscripted margins and naming answer/other. |
| 4 | MED | `04:145-150`, `04:20` | Ablation baseline 0.9975 vs the chapter's 0.9965 test-accuracy figure looked inconsistent (math-auditor confirmed they are *genuinely different* quantities: first-400 ablation subset vs full 2000-example test). | FIXED. Added a clause distinguishing the harness-subset baseline from the full-test figure. |
| 5 | MED | `04:98-100` | Dangling "spreads its weight across layers like this." set up a display that never came; numbers folded inline. | FIXED. Removed the dangling lead-in (folded into the fix for finding 1). |
| 6 | MED | `02:102-114` | Layer-1 MSB>LSB weight ordering presented as established when it is a one-run interpretation (the M=8 model retrains each run). | FIXED. Hedged to the seed-0 run; noted the bias toward more-significant bits is the stable part. |
| 7 | MINOR | `04:148` | Layer-2 ablation printed 0.763 in prose but 0.762 in the notebook's own results cell (true value 0.7625; round-half-up vs round-half-even). | FIXED. Prose now 0.762, matching the notebook results cell a reader would diff against. |
| 8 | MINOR | `02:18-21` | Three of the four single-example layer-2 weights (a=0, 5, 7) are not in the notebook's stored outputs (only a=4 is); they come from the `pointer_interp.py` per-example loop the notebook does not run. | FIXED (provenance). Added a parenthetical noting only a=4 is cached and the other three are illustrative seed-0 values, with the certified aggregate following. Not re-run (the values are correct and seed-0-reproducible; the aggregate is the load-bearing evidence and is stored). |
| - | LOW | `ch07:50-51` | "the book's flagship... largest single section in the book" praised the section from inside it (production detail leaking into prose). | FIXED. Reworded to a descriptive statement of the result. |
| - | LOW | `05:42-43` | "inner loop of something larger" forward-promise to Ch8. | LEFT AS-IS: accurate per the Part III spec (REINFORCE *is* return-weighted softmax cross-entropy; the supervised toolkit is literally RL's inner loop). |

## Verification after fixes

- `make cleanall && make`: exit 0, **100 pages** (was 98; +2 pp from the reconciliation and definition sentences). Ch7 still within its flagship budget.
- Zero undefined / multiply-defined references.
- `0.763` removed; `0.762` present; both `0.9975` and `0.9965` present and distinguished in §9.
- Zero em-dashes across ch07; Ch6 reconciliation phrase present.

## Status

**Chapter 7 PASS after fixes.** All HIGH/MEDIUM/MINOR findings resolved or consciously deferred (one LOW left as-is, justified). Ready for the Part III integration pass once Chapter 8 lands.
