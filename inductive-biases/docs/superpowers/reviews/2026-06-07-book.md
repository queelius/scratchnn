# Review: Full Book (whole-book pass)

**Date:** 2026-06-07
**Scope:** book (all 8 chapters, 3 parts, 112 pp), cross-cutting concerns only (each chapter was already reviewed individually)
**Reviewers:** three whole-book auditors dispatched in parallel from the main loop: a coherence cold-read (quality-auditor), a cross-chapter consistency sweep (general-purpose), and a citation/bibliography audit (general-purpose)
**HEAD at review:** `0427d0e` (after the Part II minors) -> fixes applied this pass on branch `inductive-biases-polish`

## Auditor verdicts

| Auditor | Verdict | Summary |
|---|---|---|
| Coherence cold-read | Coherent monograph, ship-close | Reads as one book, not eight stitched posts; the three-axes spine is introduced, developed, and paid off; one authorial voice; the `+=` and `p - onehot` motifs thread tactilely. One HIGH (a wrong section title), three MEDIUM, LOW polish. |
| Cross-chapter consistency | Strong | All running threads (three axes, the framework boundary, Solomonoff to AIXI, the `p - onehot` gradient) named consistently end to end; the prior Ch6/Ch7 depth contradiction confirmed fully resolved with no recurrence. One HIGH (the digits dataset mismatch), notation overloads at MEDIUM. |
| Citation / bibliography | PASS, publication-ready | Clean 34-to-34 citation bijection, zero orphans, zero undefined, zero uncited; all load-bearing entries verified correct (author/year/title/venue). A few cosmetic entry-type quirks only. |

No structural problems. The book is a coherent monograph with a sound, complete bibliography.

## Findings and resolutions

| # | Sev | Location | Finding | Resolution |
|---|---|---|---|---|
| 1 | HIGH | `ch04/04-limits-map-handoff.tex:100` | Section titled "The Transformer is next", but Chapter 5 is the RNN; the section body itself says "The recurrent network comes first." A front-to-back reader meets a title naming the wrong chapter. | FIXED. Retitled "What comes next: a state, then a lookup" (neutral and accurate; the section covers both the RNN and the Transformer). Label unchanged. |
| 2 | HIGH (flagged by two auditors) | `ch03/03-digits-and-control.tex:7`, `ch03/bib-notes.tex:18` | The digits experiment: Ch1 uses the sklearn subset (1437/360, MLP 97.2%), Ch3 uses the full UCI optdigits (3823/1797, "the same MLP" 96.1%), yet Ch3 prose and bib-note both claim "the same data" / "the same set." A reader sees one MLP at two accuracies under a sameness claim. | FIXED. Ch3 prose now states it is the fuller optdigits release, notes Ch1 used the smaller sklearn subset, and explains the 96.1% vs 97.2% gap by the different test sets. The bib-note now says "the fuller train/test release rather than the smaller sklearn subset used in Chapter 1." |
| 3 | MEDIUM | `ch06/01-lookup-and-task.tex` | Ch6 frames attention as content-addressable lookup but never ties it back to the "permutation-equivariant body plus positional encoding" label that Ch1/Ch4/Ch5 promised. | FIXED. Added a sentence: bare attention has no notion of position (permuting inputs permutes outputs identically), so it is exactly the permutation-equivariant body the earlier chapters anticipated, and the positional encoding restores order. |
| 4 | MEDIUM | `ch01/03-output-heads.tex:31` | `\eta` is the learning rate in Ch1 (one use) but the exponential-family natural parameter throughout Ch2 (twelve uses). Cross-chapter symbol overload. | FIXED. Ch1's single learning-rate `\eta` changed to `\alpha`; Ch2's `\eta` (natural parameter) left untouched. |
| 5 | LOW | `ch07/03-induction-and-icl.tex:135` | A parenthetical pointed at an out-of-book path (`docs/series/archive/transformer-text.md`), unreachable by a reader of the shipped monograph. | FIXED. Reworded to describe what mechanistic-interpretability studies of trained character-level models visualize, with no file path. |
| 6 | LOW | `ch08/03-gridworld.tex:5` | A `$\times$` in a section title produced three hyperref "Token not allowed in a PDF string" build warnings (PDF bookmark only). | FIXED. Wrapped in `\texorpdfstring{$5 \times 5$}{5x5}`; the three warnings are gone. |

### Consciously deferred (noted, not changed)

- **`\sigma` overload** (sigmoid in Ch1 vs Gaussian scale in Ch2): already disambiguated locally in Ch2 (`\sigma_{\mathrm{logistic}}`); not reader-visible as a contradiction. LOW, left as-is.
- **`\hat{y}` / `\hat{p}` / action**: Ch2 explicitly reconciles `\hat{y}` to `\hat{p}`; Ch8's reuse of `\hat{y}` as the action is locally unambiguous. LOW, left as-is.
- **`\Loss` macro defined but unused**: harmless dead macro; the book consistently uses plain `L`. LOW, left as-is.
- **En-dashes in numeric ranges** (e.g. "10--16%"): the house rule (README) forbids em-dashes only; en-dashes in ranges are standard. No change.
- **Bibliography entry-type quirks** (a few conference papers typed `@article`, arXiv in the `journal` field): every fact is correct and they render fine under authoryear. No change.

## Verification after fixes

- `make cleanall && make`: exit 0, **112 pages**, zero undefined references, zero undefined citations.
- The three hyperref PDF-string warnings are gone (0).
- Zero em-dashes across the seven edited files.
- Spot-checks: the wrong "Transformer is next" title is gone; the Ch3 digits reconciliation is present; Ch1's learning-rate `\eta` is gone; the out-of-book archive path is gone.

## Status

**Full-book review PASS after fixes.** The two HIGH findings (a wrong section title and the digits dataset mismatch) are resolved, the cheap cross-cutting items are fixed, and the bibliography was already publication-ready. The book is coherent as a monograph and ready to ship.
